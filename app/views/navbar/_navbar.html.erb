<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <% if root_path != request.path %>
      <%= link_to root_path do %>
        <button class='btn' style='width: 70px;'>
          <%= image_tag 'arrow.svg', class: "navbar-brand"  %>
        </button>
      <% end %>    
    <% end %>
    
    
    <a class="navbar-brand" href="#">BUDGET APP</a>
    
    <% if user_signed_in? %>
      <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "btn btn-outline-light" %>
    <% end %>
      </ul>
    </div>
  </div>
</nav>